# SOURCE 微信小程序开发计划

> **产品定位**：SOURCE 的移动入口，面向设计师和印刷从业者的随身色彩工具。  
> **核心价值**：查色、购色、扫码识色，打通线上线下色彩工作流。

---

## 产品架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                    SOURCE 微信小程序                          │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 查色中心 │  │ 色彩商城 │  │ 扫码工具 │  │ 个人中心 │        │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘        │
│       │           │           │           │               │
│  ┌────┴───────────┴───────────┴───────────┴────┐          │
│  │              SOURCE API 网关                 │          │
│  │   (色彩查询 / 订单 / 支付 / 用户 / 扫码)      │          │
│  └──────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    SOURCE 后端服务                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ 色彩服务  │  │ 商城服务  │  │ 支付服务  │  │ 用户服务  │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ 扫码服务  │  │ 推荐服务  │  │ 注册服务  │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 版本规划

| 版本 | 代号 | 核心目标 | 预估周期 |
|------|------|---------|---------|
| 1.0.0 | **Finder** | 查色中心 + 色彩预览 | 2 周 |
| 1.1.0 | **Market** | 商城 + 微信支付 + 订单管理 | 3 周 |
| 1.2.0 | **Scanner** | 扫码注册 + 相机识色 | 2 周 |
| 1.3.0 | **Profile** | 个人中心 + 完整账户体系 | 1 周 |
| 2.0.0 | **Complete** | 功能完善 + 性能优化 + 上线 | 2 周 |

---

## v1.0.0 - Finder（查色中心）

### 功能清单

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 微信登录 | 一键授权登录 | P0 |
| 色彩搜索 | 按编号/名称搜索 | P0 |
| 色彩列表 | 分页展示 + 筛选 | P0 |
| 色彩详情 | 简化版身份证预览 | P0 |
| 色彩预览 | Lab 数据展示 + 色块渲染 | P0 |
| 收藏功能 | 收藏常用色彩 | P1 |

### 页面结构

```
/pages
├── index/                 # 首页（搜索 + 热门色彩）
├── colors/
│   ├── list/              # 色彩列表
│   └── detail/            # 色彩详情
├── auth/
│   └── login/             # 登录页
└── webview/               # 跳转 Web 详情页
```

### 技术选型

| 技术 | 选择 | 理由 |
|------|------|------|
| 框架 | Taro 3.x + React | 跨端能力、React 生态复用 |
| 状态管理 | Zustand | 轻量、类型友好 |
| UI 组件 | NutUI | 京东出品、微信风格 |
| 请求库 | Taro.request 封装 | 原生 API、拦截器 |
| 样式 | Tailwind CSS (via UnoCSS) | 与 Web 端一致 |

### API 依赖

```typescript
// 已有 API
GET  /api/v1/colors                    // 色彩列表
GET  /api/v1/colors/[id]               // 色彩详情
GET  /api/v1/colors/search?q=          // 搜索

// 新增 API
POST /api/miniapp/auth/login           // 小程序登录
POST /api/miniapp/auth/phone           // 手机号绑定
GET  /api/miniapp/user/favorites       // 收藏列表
POST /api/miniapp/user/favorites       // 添加收藏
DELETE /api/miniapp/user/favorites/[id] // 取消收藏
```

### 数据模型扩展

```prisma
// 小程序用户扩展
model WechatMiniUser {
  id          String   @id @default(cuid())
  userId      String   @unique
  user        User     @relation(fields: [userId], references: [id])
  openId      String   @unique    // 微信 OpenID
  unionId     String?             // 微信 UnionID
  sessionKey  String?             // 会话密钥（加密存储）
  phoneNumber String?             // 绑定手机号
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

// 色彩收藏
model ColorFavorite {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  colorId   String
  color     Color    @relation(fields: [colorId], references: [id])
  createdAt DateTime @default(now())

  @@unique([userId, colorId])
}
```

---

## v1.1.0 - Market（色彩商城）

### 功能清单

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 商品列表 | 色谱 + 单色打样包 | P0 |
| 商品详情 | 规格、价格、描述 | P0 |
| 购物车 | 添加/删除/数量调整 | P0 |
| 微信支付 | JSAPI 支付 | P0 |
| 订单创建 | 下单流程 | P0 |
| 订单列表 | 我的订单 | P0 |
| 订单详情 | 状态追踪 | P0 |
| 色谱一键导入 | 购买后自动注册 | P1 |

### 商品类型

| 类型 | 说明 | 特殊逻辑 |
|------|------|---------|
| **色谱** (ColorSpectrum) | 包含多个色彩的套装 | 购买后生成注册码，支持一键导入 |
| **单色打样包** (ProofingPack) | 单个色彩的实体打样 | 复用现有 ProofingPack 模型 |

### 页面结构

```
/pages
├── shop/
│   ├── index/             # 商城首页
│   ├── spectrum/          # 色谱专区
│   │   ├── list/          # 色谱列表
│   │   └── detail/        # 色谱详情
│   ├── proofing/          # 打样包专区
│   │   ├── list/          # 打样包列表
│   │   └── detail/        # 打样包详情
│   └── cart/              # 购物车
├── order/
│   ├── confirm/           # 订单确认
│   ├── pay/               # 支付页
│   ├── result/            # 支付结果
│   ├── list/              # 订单列表
│   └── detail/            # 订单详情
└── address/
    ├── list/              # 地址列表
    └── edit/              # 地址编辑
```

### 数据模型

```prisma
// 色谱（色彩套装）
model ColorSpectrum {
  id           String   @id @default(cuid())
  name         String                        // 色谱名称
  description  String?                       // 描述
  coverImage   String?                       // 封面图
  price        Decimal  @db.Decimal(10, 2)   // 价格
  originalPrice Decimal? @db.Decimal(10, 2)  // 原价
  stock        Int      @default(0)          // 库存
  status       ProductStatus @default(DRAFT) // 状态
  colors       ColorSpectrumItem[]           // 包含的色彩
  orders       OrderItem[]
  registrations SpectrumRegistration[]
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

// 色谱-色彩关联
model ColorSpectrumItem {
  id          String        @id @default(cuid())
  spectrumId  String
  spectrum    ColorSpectrum @relation(fields: [spectrumId], references: [id])
  colorId     String
  color       Color         @relation(fields: [colorId], references: [id])
  sortOrder   Int           @default(0)

  @@unique([spectrumId, colorId])
}

// 订单
model Order {
  id              String      @id @default(cuid())
  orderNo         String      @unique              // 订单号
  userId          String
  user            User        @relation(fields: [userId], references: [id])
  status          OrderStatus @default(PENDING)
  totalAmount     Decimal     @db.Decimal(10, 2)   // 订单总额
  paymentAmount   Decimal     @db.Decimal(10, 2)   // 实付金额
  paymentMethod   PaymentMethod?
  paymentTime     DateTime?
  transactionId   String?                          // 微信支付交易号
  
  // 收货信息
  receiverName    String?
  receiverPhone   String?
  receiverAddress String?
  
  // 发货信息
  shippingCompany String?
  trackingNumber  String?
  shippedAt       DateTime?
  
  items           OrderItem[]
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt
}

// 订单项
model OrderItem {
  id            String         @id @default(cuid())
  orderId       String
  order         Order          @relation(fields: [orderId], references: [id])
  productType   ProductType                     // SPECTRUM | PROOFING_PACK
  spectrumId    String?
  spectrum      ColorSpectrum? @relation(fields: [spectrumId], references: [id])
  proofingPackId String?
  proofingPack  ProofingPack?  @relation(fields: [proofingPackId], references: [id])
  quantity      Int
  unitPrice     Decimal        @db.Decimal(10, 2)
  totalPrice    Decimal        @db.Decimal(10, 2)
  
  // 色谱注册码（色谱类订单项）
  registrationCode String?     @unique
  registeredAt     DateTime?
}

// 色谱注册记录
model SpectrumRegistration {
  id            String        @id @default(cuid())
  spectrumId    String
  spectrum      ColorSpectrum @relation(fields: [spectrumId], references: [id])
  userId        String
  user          User          @relation(fields: [userId], references: [id])
  orderItemId   String?       @unique
  registrationCode String     @unique
  registeredAt  DateTime      @default(now())

  @@unique([spectrumId, userId])
}

// 购物车
model CartItem {
  id            String      @id @default(cuid())
  userId        String
  user          User        @relation(fields: [userId], references: [id])
  productType   ProductType
  spectrumId    String?
  spectrum      ColorSpectrum? @relation(fields: [spectrumId], references: [id])
  proofingPackId String?
  proofingPack  ProofingPack?  @relation(fields: [proofingPackId], references: [id])
  quantity      Int         @default(1)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt

  @@unique([userId, productType, spectrumId])
  @@unique([userId, productType, proofingPackId])
}

// 收货地址
model UserAddress {
  id          String   @id @default(cuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  name        String                          // 收货人
  phone       String                          // 联系电话
  province    String                          // 省
  city        String                          // 市
  district    String                          // 区
  address     String                          // 详细地址
  isDefault   Boolean  @default(false)        // 默认地址
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

// 枚举
enum ProductStatus {
  DRAFT      // 草稿
  ACTIVE     // 上架
  SOLD_OUT   // 售罄
  OFFLINE    // 下架
}

enum ProductType {
  SPECTRUM       // 色谱
  PROOFING_PACK  // 打样包
}

enum OrderStatus {
  PENDING        // 待支付
  PAID           // 已支付
  SHIPPING       // 发货中
  DELIVERED      // 已发货
  COMPLETED      // 已完成
  CANCELLED      // 已取消
  REFUNDING      // 退款中
  REFUNDED       // 已退款
}

enum PaymentMethod {
  WECHAT_PAY     // 微信支付
}
```

### 微信支付集成

```typescript
// 支付流程
1. 用户确认订单 → 创建订单（status: PENDING）
2. 调用后端 /api/miniapp/order/prepay → 获取支付参数
3. 后端调用微信统一下单 API → 返回 prepay_id
4. 小程序端调用 wx.requestPayment
5. 支付成功 → 微信回调 /api/miniapp/payment/notify
6. 后端验签 → 更新订单状态（status: PAID）
7. 色谱订单 → 生成注册码

// 关键配置
interface WechatPayConfig {
  appId: string;           // 小程序 AppID
  mchId: string;           // 商户号
  apiKey: string;          // API 密钥
  certPath: string;        // 商户证书路径
  notifyUrl: string;       // 支付回调地址
}
```

### API 新增

```typescript
// 商品
GET  /api/miniapp/shop/spectrums           // 色谱列表
GET  /api/miniapp/shop/spectrums/[id]      // 色谱详情
GET  /api/miniapp/shop/proofing-packs      // 打样包列表
GET  /api/miniapp/shop/proofing-packs/[id] // 打样包详情

// 购物车
GET    /api/miniapp/cart                   // 购物车列表
POST   /api/miniapp/cart                   // 添加到购物车
PUT    /api/miniapp/cart/[id]              // 更新数量
DELETE /api/miniapp/cart/[id]              // 移除商品

// 订单
POST /api/miniapp/order                    // 创建订单
GET  /api/miniapp/order                    // 订单列表
GET  /api/miniapp/order/[orderNo]          // 订单详情
POST /api/miniapp/order/[orderNo]/cancel   // 取消订单
POST /api/miniapp/order/prepay             // 获取支付参数

// 支付回调
POST /api/miniapp/payment/notify           // 微信支付回调

// 收货地址
GET    /api/miniapp/address                // 地址列表
POST   /api/miniapp/address                // 新增地址
PUT    /api/miniapp/address/[id]           // 编辑地址
DELETE /api/miniapp/address/[id]           // 删除地址

// 色谱注册
POST /api/miniapp/spectrum/register        // 手动注册码注册
POST /api/miniapp/spectrum/auto-register   // 订单一键导入
GET  /api/miniapp/spectrum/my              // 我的色谱
```

---

## v1.2.0 - Scanner（扫码工具）

### 功能清单

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 扫码注册 | 扫描色谱注册码 | P0 |
| 相机识色 | 拍照取色 → Lab 转换 | P0 |
| 相似色推荐 | 基于 ΔE 匹配 | P0 |
| 取色历史 | 保存取色记录 | P1 |
| 色卡识别 | 识别标准色卡 | P2 |

### 页面结构

```
/pages
├── scan/
│   ├── index/             # 扫码入口
│   ├── register/          # 注册结果
│   └── color/             # 识色结果
└── camera/
    ├── capture/           # 拍照取色
    └── history/           # 取色历史
```

### 核心算法

```typescript
// RGB → Lab 转换（标准 D65 光源）
function rgbToLab(r: number, g: number, b: number): Lab {
  // RGB → XYZ
  let x = r * 0.4124564 + g * 0.3575761 + b * 0.1804375;
  let y = r * 0.2126729 + g * 0.7151522 + b * 0.0721750;
  let z = r * 0.0193339 + g * 0.1191920 + b * 0.9503041;
  
  // XYZ → Lab (D65 参考白)
  x /= 95.047;
  y /= 100.0;
  z /= 108.883;
  
  x = x > 0.008856 ? Math.cbrt(x) : (7.787 * x + 16 / 116);
  y = y > 0.008856 ? Math.cbrt(y) : (7.787 * y + 16 / 116);
  z = z > 0.008856 ? Math.cbrt(z) : (7.787 * z + 16 / 116);
  
  return {
    l: 116 * y - 16,
    a: 500 * (x - y),
    b: 200 * (y - z),
  };
}

// ΔE 2000 色差计算（用于相似色匹配）
function deltaE2000(lab1: Lab, lab2: Lab): number {
  // ... 标准 CIEDE2000 公式
}

// 相似色搜索
async function findSimilarColors(targetLab: Lab, limit = 5): Promise<SimilarColor[]> {
  // 1. 从数据库获取所有色彩的 Lab 值
  // 2. 计算 ΔE 2000
  // 3. 按 ΔE 升序排序
  // 4. 返回前 N 个
}
```

### 相机取色实现

```typescript
// 小程序端
Page({
  onCameraCapture() {
    const ctx = wx.createCameraContext();
    ctx.takePhoto({
      quality: 'high',
      success: (res) => {
        // 获取图片中心区域平均色值
        this.analyzeImage(res.tempImagePath);
      }
    });
  },
  
  async analyzeImage(imagePath: string) {
    // 1. 使用 Canvas 绘制图片
    // 2. 获取中心区域像素数据
    // 3. 计算平均 RGB
    // 4. 转换为 Lab
    // 5. 调用相似色 API
  }
});

// 后端 API
POST /api/miniapp/scan/color
Body: { lab: { l, a, b }, limit?: number }
Response: {
  similar: [
    { colorId, name, lab, deltaE, url },
    ...
  ]
}
```

### 扫码注册流程

```
用户扫描色谱二维码
       │
       ▼
  解析二维码内容
  (格式: source://spectrum/register?code=xxx)
       │
       ▼
  调用注册 API
       │
       ├── 已登录 → 直接注册 → 显示成功页
       │
       └── 未登录 → 保存 code → 跳转登录 → 登录后自动注册
```

### API 新增

```typescript
// 扫码
POST /api/miniapp/scan/register     // 扫码注册色谱
POST /api/miniapp/scan/color        // 识色匹配

// 取色历史
GET    /api/miniapp/color-history         // 历史记录
POST   /api/miniapp/color-history         // 保存记录
DELETE /api/miniapp/color-history/[id]    // 删除记录
```

### 数据模型

```prisma
// 取色历史
model ColorHistory {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  labL      Float                          // Lab L*
  labA      Float                          // Lab a*
  labB      Float                          // Lab b*
  imageUrl  String?                        // 原图（可选）
  matchedColorId String?                   // 匹配到的色彩
  matchedColor   Color? @relation(fields: [matchedColorId], references: [id])
  matchedDeltaE  Float?                    // 匹配色差
  note      String?                        // 备注
  createdAt DateTime @default(now())
}
```

---

## v1.3.0 - Profile（个人中心）

### 功能清单

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 个人信息 | 头像、昵称、手机号 | P0 |
| 我的色谱 | 已购买/注册的色谱 | P0 |
| 我的收藏 | 收藏的色彩 | P0 |
| 我的订单 | 订单列表入口 | P0 |
| 取色历史 | 相机取色记录 | P1 |
| 账号设置 | 手机号绑定/解绑 | P1 |
| 关于 SOURCE | 品牌介绍 | P2 |

### 页面结构

```
/pages
├── profile/
│   ├── index/             # 个人中心首页
│   ├── info/              # 个人信息编辑
│   ├── spectrum/          # 我的色谱
│   ├── favorites/         # 我的收藏
│   └── settings/          # 设置
└── about/                 # 关于 SOURCE
```

### 个人中心首页布局

```
┌─────────────────────────────────────┐
│  ┌──────┐                           │
│  │ 头像  │  昵称                     │
│  └──────┘  已绑定手机号              │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │ 我的色谱  3 本    →          │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 我的收藏  12 个   →          │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 我的订单         →          │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 取色历史  8 条    →          │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  设置  |  关于 SOURCE  |  退出登录   │
└─────────────────────────────────────┘
```

---

## 后端服务扩展

### 新增 API 路由结构

```
/api/miniapp/
├── auth/
│   ├── login              # 微信登录
│   └── phone              # 手机号绑定
├── user/
│   ├── profile            # 用户信息
│   └── favorites          # 收藏管理
├── shop/
│   ├── spectrums          # 色谱商品
│   └── proofing-packs     # 打样包商品
├── cart/                  # 购物车
├── order/                 # 订单
├── payment/
│   └── notify             # 支付回调
├── address/               # 收货地址
├── spectrum/              # 色谱注册
├── scan/                  # 扫码功能
│   ├── register           # 扫码注册
│   └── color              # 识色匹配
└── color-history/         # 取色历史
```

### tRPC Router 扩展

```typescript
// server/routers/miniapp.ts
export const miniappRouter = router({
  auth: miniappAuthRouter,
  user: miniappUserRouter,
  shop: miniappShopRouter,
  cart: miniappCartRouter,
  order: miniappOrderRouter,
  address: miniappAddressRouter,
  spectrum: miniappSpectrumRouter,
  scan: miniappScanRouter,
  colorHistory: miniappColorHistoryRouter,
});
```

---

## 管理后台扩展

### 新增功能

| 功能 | 路径 | 说明 |
|------|------|------|
| 色谱管理 | `/admin/spectrums` | 色谱 CRUD、色彩关联 |
| 订单管理 | `/admin/orders` | 订单列表、发货、退款 |
| 注册码管理 | `/admin/registrations` | 查看注册记录 |
| 商城配置 | `/admin/shop/settings` | 支付、配送设置 |

---

## 技术实现要点

### 1. 小程序项目结构

```
miniapp/
├── src/
│   ├── app.ts                    # 入口
│   ├── app.config.ts             # 全局配置
│   ├── pages/                    # 页面
│   ├── components/               # 组件
│   ├── services/                 # API 服务
│   ├── stores/                   # 状态管理
│   ├── utils/                    # 工具函数
│   └── types/                    # 类型定义
├── project.config.json           # 小程序配置
├── package.json
└── tsconfig.json
```

### 2. 微信支付服务封装

```typescript
// lib/wechat-pay.ts
class WechatPayService {
  // 统一下单
  async unifiedOrder(params: UnifiedOrderParams): Promise<PrepayResult>;
  
  // 验证回调签名
  async verifyNotify(xml: string): Promise<NotifyResult>;
  
  // 查询订单
  async queryOrder(outTradeNo: string): Promise<OrderQueryResult>;
  
  // 申请退款
  async refund(params: RefundParams): Promise<RefundResult>;
}
```

### 3. 色彩相似度服务

```typescript
// lib/color-similarity.ts
class ColorSimilarityService {
  // 基于 ΔE 2000 的相似色搜索
  async findSimilar(lab: Lab, options?: {
    limit?: number;
    maxDeltaE?: number;
    status?: ColorStatus;
  }): Promise<SimilarColor[]>;
  
  // 批量计算色差
  calculateBatchDeltaE(source: Lab, targets: Lab[]): number[];
}
```

### 4. 安全考虑

| 安全点 | 措施 |
|--------|------|
| 微信登录 | 服务端验证 code，session_key 加密存储 |
| 支付签名 | 使用商户证书，HTTPS 回调 |
| 敏感数据 | 手机号解密验证、地址加密存储 |
| 接口鉴权 | JWT Token + 请求签名 |
| 防刷机制 | 频率限制、验证码 |

---

## 里程碑与交付物

### v1.0.0 - Finder
- [ ] 小程序项目初始化（Taro + React）
- [ ] 微信登录功能
- [ ] 色彩搜索与列表
- [ ] 色彩详情页（简化版）
- [ ] 收藏功能
- [ ] 后端 miniapp API 基础

### v1.1.0 - Market
- [ ] 色谱商品管理
- [ ] 打样包商品展示
- [ ] 购物车功能
- [ ] 微信支付集成
- [ ] 订单管理
- [ ] 收货地址管理
- [ ] 色谱一键导入注册
- [ ] 后台订单管理页面

### v1.2.0 - Scanner
- [ ] 扫码注册功能
- [ ] 相机取色功能
- [ ] Lab 转换与相似色匹配
- [ ] 取色历史记录

### v1.3.0 - Profile
- [ ] 个人中心页面
- [ ] 我的色谱管理
- [ ] 账号设置
- [ ] 完善用户体验

### v2.0.0 - Complete
- [ ] 性能优化
- [ ] 异常处理完善
- [ ] 埋点统计
- [ ] 审核上线

---

## 资源需求

| 资源 | 说明 | 备注 |
|------|------|------|
| 微信小程序账号 | 企业主体 | 支持微信支付 |
| 微信支付商户号 | 已开通 JSAPI 支付 | 需企业资质 |
| SSL 证书 | 支付回调 HTTPS | 已有 |
| 服务器资源 | 与现有共用 | 无需新增 |

---

## 风险与对策

| 风险 | 对策 |
|------|------|
| 微信审核不通过 | 提前准备资质、合规文案 |
| 支付失败率高 | 完善异常处理、订单状态同步 |
| 识色精度差 | 提示用户使用标准光源、提供校准功能 |
| 色谱盗用 | 一码一用、绑定设备 |

---

*文档版本：v1.0*  
*创建时间：2026-01-09*  
*预计启动：待定*
