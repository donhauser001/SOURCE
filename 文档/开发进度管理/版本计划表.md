# SOURCE 版本计划表

> 版本号规则：`主版本.次版本.修订号`  
> - 主版本：重大架构变更或里程碑  
> - 次版本：功能模块完成  
> - 修订号：Bug 修复、小优化  

---

## 核心认知

```
SOURCE 不是一个商城
SOURCE 也不是一个插件
SOURCE 是一个"标准系统"，其它都是围绕它旋转的接口层

AI 是放大器，不是基础设施
CLI 是系统接口，不是玩具
Analyze 只吃事实，不吃想象
```

---

## 版本总览

| 版本 | 代号 | 核心目标 | 状态 |
|------|------|---------|------|
| 0.1.x | **Foundation** | 项目脚手架、基础框架、AI-ready 基建 | ✅ 完成 |
| 0.2.x | **Identity** | 色彩身份证 + 数据录入（提前减负） | 🚧 进行中 (v0.2.1 完成) |
| 0.3.x | **Bridge** | 履约桥（SKU + 购买意图 + 外链） | 待开发 |
| 0.4.x | **Access** | 权限体系、插件授权 | 待开发 |
| 0.5.x | **Admin** | 管理后台（订单、用户、统计） | 🚧 部分提前完成 |
| 0.6.x | **Analyze** | 工程色彩分析系统 | 待开发 |
| 1.0.0 | **Genesis** | 正式版发布 | 待开发 |

---

## v0.1.x - Foundation（基础框架）

### v0.1.0 - 项目脚手架

**目标**：搭建完整的项目骨架，可运行的空壳应用。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| Next.js 15 项目初始化 | App Router + TypeScript | `pnpm dev` 可启动 |
| Monorepo 结构 | pnpm workspace | `apps/web`、`packages/*` 目录结构 |
| shadcn/ui 集成 | 基础组件库 | Button、Card 等组件可用 |
| Tailwind CSS 配置 | 主题色、字体、间距 | 设计系统基础变量 |
| ESLint + Prettier | 代码规范 | `pnpm lint` 通过 |
| 基础布局 | Header、Footer、侧边栏骨架 | 页面框架可见 |

**交付物**：
- [x] 项目仓库初始化
- [x] 基础目录结构
- [x] 开发环境可运行

---

### v0.1.1 - 数据库与 ORM

**目标**：数据层基础设施就绪。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| PostgreSQL 配置 | 本地/Docker 开发环境 | 数据库可连接 |
| Prisma 集成 | ORM + 类型生成 | `prisma db push` 成功 |
| 核心数据模型 | Color、PaperProfile、Batch 等 | Schema 定义完成 |
| 种子数据 | 测试用色彩数据 | `prisma db seed` 可执行 |

**交付物**：
- [x] `prisma/schema.prisma` 完整定义
- [x] 数据库迁移脚本
- [x] 种子数据脚本

---

### v0.1.2 - API 层与认证

**目标**：API 基础设施 + 用户认证系统。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| tRPC 集成 | 路由配置、类型安全 | 示例 API 可调用 |
| NextAuth.js | 邮箱登录、OAuth | 登录/注册流程完整 |
| 会话管理 | JWT / Session | 登录态保持 |
| 用户模型 | User 表 + Profile | 用户数据可存储 |

**交付物**：
- [x] tRPC Router 骨架
- [x] 认证页面（登录/注册）
- [x] 用户设置页面

---

### v0.1.3 - AI-ready 基建

**目标**：为未来 AI/CLI 接入预留接口。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| ApiKey 模型 | 密钥管理表 | CRUD 完成 |
| ApiKey 中间件 | 鉴权校验 | 无效 Key 返回 401 |
| Scope 枚举 | 权限范围定义 | 常量文件完成 |
| 错误码规范 | 统一错误格式 | 全局错误处理 |
| CliAuditLog 模型 | 审计日志表 | 表结构完成 |

**交付物**：
- [x] ApiKey 管理 API
- [x] 统一错误响应格式
- [x] 审计日志记录函数

---

### v0.1.4 - SOURCE CLI 骨架

**目标**：CLI 工具基础框架。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| CLI 包初始化 | `packages/cli` | `source --help` 可执行 |
| 命令框架 | Commander.js / Yargs | 子命令结构 |
| JSON 输出 | `--json` flag | 结构化输出 |
| ApiKey 认证 | 环境变量/配置文件 | 认证流程完成 |
| 错误处理 | 统一错误码 | 错误输出规范 |

**交付物**：
- [x] `source` 命令可执行
- [x] `source --version`
- [x] `source --help`

---

## v0.2.x - Identity（色彩身份证 + 数据录入）

> **调整说明**：将 Admin 的"数据录入子集"提前到此阶段，极大减轻后续负担。

### v0.2.0 - 色彩数据 CRUD

**目标**：色彩数据的完整增删改查。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| Color CRUD API | tRPC procedures | 增删改查接口 |
| PaperProfile CRUD | 纸张表现数据 | 关联 Color |
| Batch 管理 | 验证批次 | 批次创建/查询 |
| 数据验证 | Zod schema | 输入校验完整 |

**交付物**：
- [x] `color.create` / `color.update` / `color.delete` / `color.get` / `color.list`
- [x] `paperProfile.*` 系列 API
- [x] `batch.*` 系列 API

---

### v0.2.1 - 色彩身份证页面 ✅

**目标**：每个 ColorID 的专属展示页面。

| 功能 | 说明 | 验收标准 | 状态 |
|------|------|---------|------|
| 身份证页面 | `/color/[id]` | SSG 静态生成 | ✅ |
| 基础档案区 | 编号、名称、Lab 真源 | 数据展示完整 | ✅ |
| 实操表现区 | 多纸张扫描对比 | 切换纸张可见 | ✅ |
| 材质雷达图 | 光泽度/吸墨率/色域 | 图表渲染 | ✅ |
| 生产技术区 | 油墨配方、容差 | 配方展示 | ✅ |
| SEO 优化 | Meta、结构化数据 | 搜索引擎可索引 | ✅ |

**交付物**：
- [x] 色彩身份证页面组件 (`color-identity-card.tsx`)
- [x] 响应式布局
- [x] Open Graph 预览
- [x] `/colors` 色彩列表页
- [x] `/docs` 文档首页（含子页面：API、CLI、色彩身份证）
- [x] `/analyze` 工程分析占位页
- [x] `/partners` 合作者列表页（额外完成）
- [x] `/partners/[id]` 合作者详情页（额外完成）

---

### v0.2.2 - 色彩搜索 🚧

**目标**：快速查找色彩。

| 功能 | 说明 | 验收标准 | 状态 |
|------|------|---------|------|
| 搜索 API | 模糊匹配 | 编号/名称可搜 | ✅ |
| 搜索 UI | 搜索框 + 结果列表 | 即时响应 | 🚧 基础完成 |
| 筛选器 | 纸张类型、推荐等级 | 组合筛选 | ⏳ 待开发 |
| 色彩列表页 | `/colors` | 分页展示 | ✅ |

**交付物**：
- [x] 搜索 API（`/api/v1/colors` 支持 `q` 参数）
- [x] 色彩列表页 (`/colors`)
- [ ] 筛选器组件（纸张类型、推荐等级）
- [ ] 搜索结果高亮

---

### v0.2.3 - CLI 色彩命令 🚧

**目标**：CLI 支持色彩查询。

| 功能 | 说明 | 验收标准 | 状态 |
|------|------|---------|------|
| `source color get` | 获取色彩身份证 | JSON 输出完整 | ✅ |
| `source color list` | 获取色彩列表 | 分页支持 | ✅ |
| `source color paper` | 获取纸张表现 | 带 citations | ✅ |
| `source color recommend` | 纸张推荐 | 规则模型输出 | ⏳ 待开发 |
| `source search` | 搜索色彩 | 支持模糊匹配 | ✅ |
| `source cite` | 生成引用 | 标准格式 | ⏳ 待开发 |
| `source config` | 配置管理 | API Key 设置 | ✅ |

**交付物**：
- [x] `source color get <colorId>` 命令
- [x] `source color list` 命令
- [x] `source color paper <colorId>` 命令
- [x] `source search <query>` 命令
- [x] `source config set-key/show` 命令
- [ ] `source color recommend` 命令
- [ ] `source cite` 命令
- [ ] 审计日志记录（API 端已支持）

---

### v0.2.4 - 数据录入工具（提前减负）🚧

**目标**：内部数据维护工具，减轻后续每一步的负担。

| 功能 | 说明 | 验收标准 | 状态 |
|------|------|---------|------|
| 色彩录入表单 | Color 基础字段 | 表单可用 | ✅ |
| 色彩编辑表单 | 编辑已有色彩 | 完整字段 | ✅ |
| 色彩列表管理 | 后台色彩列表 | 增删改查 | ✅ |
| 合作者管理 | Partner CRUD | 类型/状态管理 | ✅ |
| PaperProfile 录入 | 关联色彩 + 纸张 | 批量添加 | ⏳ 待开发 |
| Batch 创建 | 批次信息录入 | 关联色彩 | 🚧 API 完成 |
| CSV/JSON 导入 | 批量数据导入 | 格式校验 + 错误提示 | ⏳ 待开发 |
| 数据预览 | 导入前预览 | 确认后写入 | ⏳ 待开发 |

**交付物**：
- [x] `/admin` 后台仪表盘（统计概览）
- [x] `/admin/colors` 色彩列表管理
- [x] `/admin/colors/new` 新增色彩表单
- [x] `/admin/colors/[id]/edit` 编辑色彩表单
- [x] `/admin/partners` 合作者列表管理
- [x] 后台侧边栏导航组件
- [x] 后台权限验证（ADMIN/OPERATOR）
- [ ] CSV 导入功能
- [ ] JSON 导入功能
- [ ] PaperProfile 批量录入界面

> **注意**：后台框架已提前完成，为 v0.5.x Admin 阶段减负。

---

## v0.3.x - Bridge（履约桥）

> **调整说明**：SOURCE 不做电商系统。交易在淘宝完成，SOURCE 只保留"购买语义与订单事实"。  
> 删除：购物车、订单系统、支付集成  
> 保留：SKU 定义、购买意图、外链跳转、成本估算

### v0.3.0 - SKU 定义

**目标**：打样包商品定义（不含交易）。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| ProofingPack 模型 | SKU 定义 | 一纸一包 |
| SKU 管理 API | CRUD | 增删改查 |
| 价格信息 | 参考价格（非交易价） | 价格字段 |
| 淘宝链接 | 外部履约链接 | URL 字段 |
| SKU 展示 | 色彩身份证页内嵌 | 购买入口可见 |

**交付物**：
- [ ] ProofingPack CRUD API
- [ ] SKU 数据结构
- [ ] 色彩页内的 SKU 展示组件

---

### v0.3.1 - 购买意图记录

**目标**：记录用户购买意图，跳转外部履约。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| BuyIntent 模型 | 购买意图记录 | 用户 + SKU + 时间 |
| 跳转前记录 | 点击"购买"时记录 | 数据可追溯 |
| 外链跳转 | 跳转淘宝/其他平台 | 带追踪参数 |
| 意图统计 | 热门 SKU 统计 | 简单报表 |

**交付物**：
- [ ] BuyIntent API
- [ ] 购买按钮组件（记录 + 跳转）
- [ ] 简单统计接口

> **关键**：SOURCE 不处理支付、不管库存、不发货。只记录"谁想买什么"。

---

### v0.3.2 - CLI 成本估算

**目标**：CLI 支持成本估算。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| `source cost estimate` | 成本估算命令 | JSON 输出 |
| 成本模型 | 规则计算 | 区间 + 假设 |
| 参数支持 | 颜色/纸张/数量/尺寸 | 完整参数 |
| 假设声明 | 输出包含假设条件 | 不给确定报价 |

**交付物**：
- [ ] 成本估算命令
- [ ] 成本模型配置文件
- [ ] 假设条件模板

---

## v0.4.x - Access（权限体系）

### v0.4.0 - 激活码系统

**目标**：实体书激活码验证。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| ActivationCode 模型 | 激活码表 | 一码一用 |
| 激活码生成 | 批量生成 | 管理接口 |
| 激活流程 | 用户激活 | 绑定用户 |
| 权限升级 | 激活后解锁 | 权限变更 |

**交付物**：
- [ ] 激活码 CRUD API
- [ ] 激活页面
- [ ] 权限升级逻辑

---

### v0.4.1 - 插件授权 API

**目标**：Adobe 插件授权接口。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| 插件 ApiKey | 专用密钥类型 | scope 限制 |
| 授权验证 API | `/api/plugin/verify` | 返回权限等级 |
| 色彩数据 API | 插件专用端点 | 按权限返回 |
| 速率限制 | 限流策略 | 超限返回 429 |

**交付物**：
- [ ] 插件授权 API
- [ ] 插件色彩数据 API
- [ ] 速率限制实现

---

### v0.4.2 - CLI 权限校验

**目标**：CLI 完整权限体系。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| Scope 校验 | 命令级权限 | 越权返回 403 |
| Role 支持 | ai-readonly 等 | 角色生效 |
| 审计日志 | 完整记录 | 可追溯 |

**交付物**：
- [ ] CLI 权限中间件
- [ ] 完整审计日志

---

## v0.5.x - Admin（管理后台）

> **说明**：后台框架已在 v0.2.4 提前完成，此阶段专注于运营管理功能增强。

### v0.5.0 - 后台框架 ✅ (已在 v0.2.4 提前完成)

**目标**：管理后台基础框架。

| 功能 | 说明 | 验收标准 | 状态 |
|------|------|---------|------|
| 后台布局 | `/admin/*` | 侧边栏导航 | ✅ |
| 管理员认证 | 角色校验 | 非管理员拒绝 | ✅ |
| 仪表盘 | 数据概览 | 关键指标 | ✅ |

**交付物**：
- [x] 后台布局组件 (`AdminSidebar`)
- [x] 管理员路由守卫 (`admin/layout.tsx`)
- [x] 仪表盘页面（色彩/合作者/用户/配方/批次统计）

---

### v0.5.1 - 数据管理增强

**目标**：在 v0.2.4 基础上增强数据管理能力。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| 色彩列表管理 | 表格 + 搜索 + 筛选 | 完整管理 |
| 批量操作 | 批量编辑/删除 | 多选操作 |
| 审计注记管理 | AuditNote CRUD | 顾问注记 |
| 数据导出 | CSV/JSON 导出 | 备份功能 |

**交付物**：
- [ ] 完整数据管理页面
- [ ] 批量操作功能
- [ ] 导出功能

---

### v0.5.2 - 用户与权限管理

**目标**：用户管理与 ApiKey 管理。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| 用户列表 | 查看用户 | 权限等级 |
| ApiKey 管理 | 查看/撤销 | 管理操作 |
| 激活码管理 | 生成/查看 | 批量操作 |
| BuyIntent 统计 | 购买意图分析 | 热门 SKU |

**交付物**：
- [ ] 用户管理页面
- [ ] ApiKey 管理页面
- [ ] 激活码管理页面
- [ ] 购买意图统计

---

## v0.6.x - Analyze（工程分析）

> **铁律**：Analyze 只能建立在"已有验证数据"之上，绝不能为了功能完整而提前做推断。  
> 顺序：先解析 → 再规则推荐 → 最后报告展示。守住边界。

### v0.6.0 - SourcePack 解析

**目标**：工程色彩包上传与解析。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| 上传入口 | `/analyze` | 文件上传 |
| 格式校验 | JSON Schema | 格式错误提示 |
| 解析服务 | 数据提取 | 结构化数据 |
| 颜色映射 | ColorID 匹配 | 已验证/未验证/未映射 |

**交付物**：
- [ ] 上传页面
- [ ] 解析服务
- [ ] 颜色映射逻辑

> **边界**：未映射颜色必须标注"未验证颜色，建议先映射或打样"，不猜测。

---

### v0.6.1 - 推荐引擎

**目标**：规则推荐引擎实现（不用 AI）。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| 评分模型 | 四维评分（还原度/风险/成本/用途） | 配置化 |
| 风险识别 | 大色块/渐变/叠印 | 风险标签 |
| 推荐生成 | Top 3 + 避坑 | 带证据 |
| 报告存储 | AnalysisReport | 数据持久化 |

**交付物**：
- [ ] 推荐引擎服务
- [ ] 评分配置文件
- [ ] 证据链生成

> **边界**：每条推荐必须能回溯到：颜色 → PaperProfile → Batch → 审计注记。

---

### v0.6.2 - 分析报告页面

**目标**：工程分析报告展示。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| 报告页面 | `/analyze/[id]` | 完整展示 |
| 颜色总览 | 使用颜色列表 | 验证状态 |
| 风险点 | 风险列表 | 建议展示 |
| 推荐列表 | Top 3 纸张 | 评分+依据 |
| 避坑提醒 | 不推荐纸张 | 原因展示 |
| 权限过滤 | 免费/付费 | 内容分级 |

**交付物**：
- [ ] 分析报告页面
- [ ] 权限过滤逻辑
- [ ] 报告分享/导出

---

### v0.6.3 - CLI 分析命令

**目标**：CLI 支持工程分析。

| 功能 | 说明 | 验收标准 |
|------|------|---------|
| `source analyze` | 分析命令 | JSON 输出 |
| 文件输入 | `--file` 参数 | 本地文件 |
| 报告输出 | 完整报告 | 带 citations |

**交付物**：
- [ ] `source analyze` 命令
- [ ] 报告 JSON 输出

---

## v1.0.0 - Genesis（正式版）

### 发布准备

| 任务 | 说明 | 验收标准 |
|------|------|---------|
| 全功能测试 | E2E 测试 | 核心流程通过 |
| 性能优化 | 加载速度 | LCP < 2.5s |
| 安全审计 | 漏洞扫描 | 无高危漏洞 |
| 文档完善 | API 文档、用户手册 | 文档齐全 |
| 部署配置 | 生产环境 | 稳定运行 |

### 正式发布功能清单

- [ ] 色彩身份证系统（查看、搜索）
- [ ] 数据录入工具（内部使用）
- [ ] 履约桥（SKU + 购买意图 + 外链）
- [ ] 插件授权体系（激活码、ApiKey）
- [ ] SOURCE CLI（7+ 命令）
- [ ] 管理后台（数据管理、用户管理）
- [ ] 工程色彩分析（上传、推荐、报告）

---

## 版本时间线（预估）

```
2026-01
├── v0.1.0 项目脚手架 ✅
├── v0.1.1 数据库与 ORM ✅
├── v0.1.2 API 层与认证 ✅
├── v0.1.3 AI-ready 基建 ✅
└── v0.1.4 SOURCE CLI 骨架 ✅

2026-01 ~ 2026-02 ← 当前进度
├── v0.2.0 色彩数据 CRUD ✅
├── v0.2.1 色彩身份证页面 ✅ (含合作者页面)
├── v0.2.2 色彩搜索 🚧 (API 完成，UI 筛选待开发)
├── v0.2.3 CLI 色彩命令 🚧 (5/7 命令完成)
└── v0.2.4 数据录入工具 🚧 (后台框架完成，导入功能待开发)

2026-02 ~ 2026-03
├── v0.3.0 SKU 定义
├── v0.3.1 购买意图记录
└── v0.3.2 CLI 成本估算
    ↑ 轻量化：不做购物车、不做支付

2026-03 ~ 2026-04
├── v0.4.0 激活码系统
├── v0.4.1 插件授权 API
└── v0.4.2 CLI 权限校验

2026-04 ~ 2026-05
├── v0.5.0 后台框架 ✅ (已提前完成)
├── v0.5.1 数据管理增强
└── v0.5.2 用户与权限管理

2026-05 ~ 2026-06
├── v0.6.0 SourcePack 解析
├── v0.6.1 推荐引擎
├── v0.6.2 分析报告页面
└── v0.6.3 CLI 分析命令

2026-06 ~ 2026-07
└── v1.0.0 正式版发布
```

---

## 调整记录

| 日期 | 调整内容 | 原因 |
|------|---------|------|
| 2026-01-07 | 将数据录入提前到 v0.2.4 | 减轻数据维护负担，越早减负越好 |
| 2026-01-07 | Commerce → Bridge，删除购物车/支付 | 交易在淘宝完成，SOURCE 不做电商 |
| 2026-01-07 | 强调 Analyze 边界 | 只吃事实，不吃想象 |
| 2026-01-07 | 实现《色彩身份证字段规范 v1.0》 | 新增 Paper、Recipe、FitMatrix、Risk 等模型 |
| 2026-01-07 | 实现共建合作者体系 | Partner（印厂/纸商/油墨商）+ ColorParticipation + 5 级角色体系 |
| 2026-01-08 | v0.2.1 完成，后台框架提前完成 | 色彩身份证页面、合作者页面、后台仪表盘全部就绪 |
| 2026-01-08 | CLI 命令大部分完成 | color get/list/paper、search、config 命令已实现 |
| 2026-01-08 | 代码库首次推送至 GitHub | https://github.com/donhauser001/SOURCE |

---

## 版本发布检查清单

每个版本发布前必须完成：

- [ ] 功能开发完成
- [ ] 单元测试通过
- [ ] 代码审查完成
- [ ] 文档更新
- [ ] CHANGELOG 更新
- [ ] 版本号更新
- [ ] 部署测试环境验证
- [ ] 部署生产环境

---

*文档结束*
